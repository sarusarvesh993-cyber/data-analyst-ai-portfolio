# Optional: enables the live Hugging Face stakeholder-brief call.
# The app and all calculations work without this value.
# Use a fine-grained token limited to Inference Providers; never commit .env.
HF_TOKEN=

# Optional model override. Availability depends on current inference providers.
HF_MODEL=google/gemma-2-2b-it
