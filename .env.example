# OPTIONAL — enables the LIVE LLM insight layer.
# Get a free token at https://huggingface.co/settings/tokens (no cost).
# Leave blank to use the bundled authored narrative (still fully functional, zero cost).
HF_TOKEN=
